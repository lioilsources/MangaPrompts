// Template for lib/config/secrets.dart (which is GITIGNORED).
// Copy to secrets.dart and fill in your Cloudflare Access service-token creds
// for comfyui.ol1n.com. These become the default CF creds when SharedPreferences
// has none, so you don't have to re-enter them in Settings.
class Secrets {
  static const cfAccessClientId = ''; // e.g. xxxxxxxx.access
  static const cfAccessClientSecret = '';
}
